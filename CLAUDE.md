- no need to run all tests, I can do it manually, just tell me.
- for UI based tests, I will test manually
- when asked to test the came using the terminal use `uv run dnd-game`
create a new game
select players from existing list
begin adventure in the crypt
save slot 4