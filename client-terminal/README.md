# D&D Terminal Client

Terminal-based client for the D&D 5E game engine, providing a rich text interface using the Rich library.

## Dependencies

This package depends on `dnd-engine` for game rules and logic.
